rules:
  validators:
    seaf.ta.services.backup.existence:
      uid: backup-error-existence
      description: >
        Должны быть определены и описаны системы резервного копирования
      title: Наличие системы резервного копирования
      source: >
        (
              $domain := sber.domain;
              $fn := $.seaf_dzo_core."validation.functions"."backup";
              $fn_dzo := $.seaf_dzo_core."validation.functions"."backup_dzo";
              $result := $eval($fn);
              $result_dzo := $eval($fn_dzo);
          
              [ $result.iserrors = true and $result_dzo.iserrors = true ? (
                  {
                      "uid": $self.uid & '-01',
                      "title": "Отсутствуют системы резервного копирования",
                      "location": "entities/seaf.ta.services.backup/registry?domain=" & $domain,
                      "correction": 'Необходимо заполнить сущность "seaf.ta.services.backup" или "seaf.ta.services.backup_dzo" системы резервного копирования компании',
                      "description": $self.description
                  }
              )]        
        )